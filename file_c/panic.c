/*----------------------------------------------------*
 *打印死机信息, 无限循环死机.
 *----------------------------------------------------*/
#include  "..\include\color.h"

void panic(char *szerror)
{
	printk(RED, szerror);  /* 内核中执行for(;;)将死机 */
	for(;;)
		;
}